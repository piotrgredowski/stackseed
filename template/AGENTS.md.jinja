# Agent Guidance

## Project context

This repository was generated from a generic Copier template.

Selected stacks:
- Backend: {{ backend }}
- Backend mode: {{ backend_mode }}
- Frontend: {{ frontend }}
- Config scaffold: {% if backend in ["python", "go"] and backend_mode == "cli" and config_enabled == "true" %}enabled{% else %}disabled{% endif %}

## Scratchpad policy

Use `.scratchpad/` as the approved place for temporary scripts, experiments, and
throwaway work. The directory is kept in version control with `.gitkeep`, while
other scratchpad contents are ignored.

## Documentation policy

Keep `README.md` up to date when project layout, commands, stack behavior, or
validation requirements change. Treat it as the canonical project documentation.

## Validation

{% if backend in ["python", "go"] and frontend == "solid_tailwind_shadcn" -%}
Run the stack-specific commands documented in `README.md` before handing off changes.
Run backend commands from `backend/` and frontend commands from `frontend/`.
{% else -%}
Run the stack-specific commands documented in `README.md` before handing off changes.
{% endif %}

Do not put provider credentials, API keys, or external secrets in tracked files.
